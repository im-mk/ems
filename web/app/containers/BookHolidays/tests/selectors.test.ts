// import { selectBookHolidaysDomain } from '../selectors';

describe('selectBookHolidaysDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
